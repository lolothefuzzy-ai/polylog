#include "determinism.h"\n\nnamespace polylog::core {\n\nthread_local std::atomic<bool> DeterminismMode::enabled_(false);\n\nvoid DeterminismMode::SeedAll(uint64_t seed) {\n    // Create base RNG with provided seed\n    DeterministicRNG rng(seed);\n    \n    // TODO: Seed subsystems\n    // physics::PhysicsEngine::GetInstance()->SetSeed(rng.Next());\n    // particles::ParticleSystem::GetInstance()->SetSeed(rng.Next());\n    // animation::AnimationEngine::GetInstance()->SetSeed(rng.Next());\n    // render::RenderEngine::GetInstance()->SetSeed(rng.Next());\n}\n\n}  // namespace polylog::core\n