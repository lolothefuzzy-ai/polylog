# System Restart Required

Before continuing with Tauri development:

1. Verify Rust installation at `C:\Users\Nauti\.cargo\bin`
2. Add this path to your system PATH variable
3. Restart your IDE and computer

After restarting, re-run the build script:
```powershell
./build.ps1
```
